# Local API development without Nginx.
# Usage: docker compose -f docker-compose.dev.yml up --build

services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - "${PORT:-8787}:8787"
    restart: unless-stopped
