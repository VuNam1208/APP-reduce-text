# Same Wi-Fi / emulator
SUMMARY_API_URL=http://10.11.60.254:8787

# Android emulator only
# SUMMARY_API_URL=http://10.0.2.2:8787

# ngrok (test online without same Wi-Fi; URL changes each ngrok restart on free tier)
# SUMMARY_API_URL=https://xxxx-xxxx.ngrok-free.app

# Production (VPS + domain + Nginx + HTTPS)
# SUMMARY_API_URL=https://api.yourdomain.com
